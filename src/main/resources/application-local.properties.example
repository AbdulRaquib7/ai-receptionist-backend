# Copy to application-local.properties and add your OpenAI key.
# Run with: mvn spring-boot:run -Dspring.profiles.active=local
#
# Alternatively use env (recommended): OPENAI_API_KEY=sk-xxx mvn spring-boot:run
# Env var overrides file - avoids token overwrite on deploy.

openai.api-key=sk-your-valid-openai-api-key-here
