# PostgreSQL for production. Activate with: -Dspring.profiles.active=prod
spring.datasource.url=jdbc:postgresql://localhost:5432/ai-receptionists-twilio
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# OpenAI: use OPENAI_API_KEY env or inherits from application.properties
